
# cmds/functions

set hidden

# Bindings
#
# '$' to run a shell cmd
# '&' to run an async shell cmd

map <enter> shell
map o &open "$f"
map gg top
map ge bottom
map x cut
map s toggle
map . set hidden!
